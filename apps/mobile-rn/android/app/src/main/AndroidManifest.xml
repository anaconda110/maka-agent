<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required: network access for runtime-host (cloud model) connections. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Required: voice input via microphone. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Storage: scoped (API 24-32) and legacy fallback for older devices. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- API 33+: granular media access (image/audio/video) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />

    <!-- Notifications (API 33+). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground service for long-running runtime-host sessions (optional, future).
         Android 14 (targetSdk 34) requires a foregroundServiceType on the <service>
         element plus the matching FOREGROUND_SERVICE_<type> sub-permission. The
         intended use is long-running data-sync work (runtime-host sessions), so
         the dataSync sub-permission is declared; the <service> element (with
         android:foregroundServiceType="dataSync") will be added when the service
         is implemented. Declaring the sub-permission now keeps the manifest
         Android-14-compliant the moment the service is wired up. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|resizeableLayout|uiMode|navigation"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>