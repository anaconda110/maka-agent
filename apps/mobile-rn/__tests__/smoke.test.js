/**
 * @format
 */
import { describe, expect, it } from '@jest/globals';

describe('scaffold smoke', () => {
  it('exposes the package name', () => {
    expect('@maka/mobile-rn').toBe('@maka/mobile-rn');
  });
});